language: node_js
node_js:
  - "0.11"
notifications:
  irc:
    channels:
      - "chat.freenode.net#scottgonzalez"
    template:
      - "%{author} updated the %{branch} branch of %{repository}"
      - "%{message} %{build_url}"
    skip_join: true
